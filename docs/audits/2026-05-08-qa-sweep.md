# QA Sweep #2 — 2026-05-08

**Date run:** 2026-05-08 (Fri)
**Author:** Claude Code (chore/qa-sweep-2026-05-08)
**Branch / commit:** `7c5e5d4` (scaffold), executed at `f757bc5`

Severity legend: `[BLOCKER]` fix in-week · `[SOON]` next sprint · `[BACKLOG]` Monday triage.

## 1. Lint / Format / Types

### Backend
- `ruff check .`: **0 issues** — "All checks passed!" Severity: none
- `ruff format --check .`: **102 files would be reformatted**, 41 already formatted. Severity: `[SOON]` (auto-fixable: `ruff format .`)
- `mypy . --ignore-missing-imports` (loose, no project config): **1134 errors in 114 of 143 files**. Dominant rule: `[no-untyped-def]` (missing annotations on test fixtures, route handlers, helpers). Representative:
  - `tests/integration/test_chat_endpoint.py:78,92,95` — missing annotations
  - `tests/integration/conftest.py:14,21` — missing return types
  - `tests/e2e/test_smoke.py:21,26,38,58` — missing annotations
  - Severity: `[BACKLOG]` (no project mypy config; loose-mode noise. Adding strict gradual typing is multi-sprint work.)

### Frontend
- `tsc --noEmit`: **CANNOT RUN** — `frontend/tsconfig.json` does not exist (file never committed; `git ls-files` empty). Vite uses esbuild for `.ts/.tsx` transpile, so files compile, but **TypeScript is never type-checked**. CLAUDE.md claims TS strict mode — drift. Severity: `[BLOCKER]` (project advertises TS strict; no enforcement = silent type drift across all `.tsx` components).
- `eslint`: no eslint config present (`.eslintrc*` / `eslint.config.*` absent), no `lint` script in `frontend/package.json`. Severity: `[BACKLOG]` (add eslint config + `npm run lint` script).

### Section 1 in-week blockers
- Missing `frontend/tsconfig.json` — restore (was at one point present, see CLAUDE.md). One-line fix: `npx tsc --init` then tune to `strict: true`, `jsx: "react-jsx"`, lib `es2020,dom`. Re-run tsc to surface real backlog.

---

## 2. Tests & Coverage

### Backend (pytest)
| metric | value |
|--------|-------|
| tests run | 476 |
| passing | 474 |
| skipped | 2 |
| failing | 0 |
| total coverage | not measured this run (skipped `--cov` for speed; prior runs captured in CI) |

Failing tests: none.
Warnings: 6 (Pydantic V1 `@validator` deprecation in `backend/routes/query.py:144,155`; `pythonjsonlogger.jsonlogger` moved to `pythonjsonlogger.json`; `datetime.utcfromtimestamp` deprecated in `influxdb_client`). Severity: `[SOON]`.

Severity overall: backend tests healthy. Pydantic V1 → V2 migration of `query.py` validators is a one-file fix `[SOON]`.

### Frontend (jest)
| metric | value |
|--------|-------|
| suites run | 18 |
| suites passing | 14 |
| suites failing | **4** |
| tests run | 91 |
| passing | 87 |
| failing | **4** |
| stmts/branches/funcs/lines | 58.52% / 44.48% / 45.16% / 59.67% |

Failing suites (all on main as of `f757bc5`):
- `src/__tests__/RawScoreRelationChart.test.tsx` — `renders a ReferenceArea per off period` (`toHaveLength` mismatch)
- `src/__tests__/AHURankingsTable.test.tsx` — `renders AHU human labels` (text "AHU-L1-ES-01 — Engineering Services" not found)
- `src/__tests__/CombinedScoresChart.test.tsx` — `renders a ReferenceArea for each off period` (`toHaveLength`)
- `src/__tests__/HealthIndexChart.test.tsx` — `renders a ReferenceArea for each off period when offPeriods provided` (`toHaveLength`)

Severity: `[BLOCKER]` — failing tests on main. Three are the same off-period `ReferenceArea` assertion → likely a single regression in shared off-period helper (`utils/offPeriodAreas.tsx` only at 70% coverage). The AHU label test suggests display-name format drift.

Coverage gaps (vs ~70% bar):
- `api/client.ts`: 22.95% stmts — main API surface barely tested. `[BACKLOG]` target 70%.
- `store/useAppStore.ts`: 39.47% stmts, **0% branches** — core state. `[BACKLOG]` target 70%.
- `components/dashboard/AlertsModal.tsx`: 27.9% — large untested modal. `[BACKLOG]`.
- `components/nav/FilterBar.tsx`: 38.18%. `[BACKLOG]`.
- `components/chat/ChatWindow.tsx`: 53.33%. `[BACKLOG]`.

### Section 2 blockers
- 4 failing frontend tests on main — must fix or revert source change before next deploy. Likely root cause: `offPeriodAreas` helper signature/output regression + `deviceLabel` format mismatch.

---

## 3. Dependencies

### Python
- Outdated majors (≥2 behind, prod-relevant):
  - `starlette` 0.37.2 → 1.0.0 (FastAPI compat — verify before bump) `[BACKLOG]`
  - `uvicorn` 0.29.0 → 0.46.0 (many minors behind; not 2 majors) `[BACKLOG]`
  - `transformers` 5.3.0 → 5.8.0; `sentence-transformers` 5.3.0 → 5.4.1 (RAG stack) `[BACKLOG]`
  - `xgboost` 2.0.3 → 3.2.0 (major) `[BACKLOG]`
  - `tree-sitter-language-pack` 0.13.0 → 1.6.2 (dev tool, multi-major) `[BACKLOG]`
  - `python-dotenv` 1.0.1 → 1.2.2 `[BACKLOG]`
- pip-audit: **not run** — `pip-audit` not installed in venv. Severity: `[SOON]` install + run.

### Node
- npm outdated majors (≥2 behind):
  - `react` 18.3.1 → 19.2.6 `[BACKLOG]`
  - `react-dom` 18.3.1 → 19.2.6 `[BACKLOG]`
  - `@types/react` 18.3.28 → 19.2.14, `@types/react-dom` 18.3.7 → 19.2.3 (paired with React 19) `[BACKLOG]`
  - `tailwindcss` 3.4.19 → 4.2.4 (major; v4 is a CSS-engine rewrite — non-trivial) `[BACKLOG]`
  - `@vitejs/plugin-react` 4.7.0 → 6.0.1 `[BACKLOG]`
  - `vite` 7.3.1 → 8.0.11 `[BACKLOG]`
  - `recharts` 2.15.4 → 3.8.1 (charts API shifts) `[BACKLOG]`
  - `react-markdown` 9 → 10 `[BACKLOG]`
  - `typescript` 5.9.3 → 6.0.3 `[BACKLOG]`
  - `uuid` 13 → 14 `[BACKLOG]`
- npm audit (--omit=dev): **4 vulnerabilities (2 moderate, 2 high)**:
  - `lodash <=4.17.23` — high (code injection via `_.template`, prototype pollution). `[SOON]` (transitive; check reachable surface)
  - `follow-redirects <=1.15.11` — moderate (auth header leak on cross-domain redirect; transitive via `axios`). `[SOON]`
  - `uuid 13.0.0` — moderate (buffer bounds). `[SOON]`
  - All "fix available via `npm audit fix`" — try in branch; verify tests still pass.

### Section 3 blockers
- None reachability-confirmed BLOCKER. The lodash high-severity is transitive — investigate whether reachable from prod bundle before promoting to BLOCKER.

---

## 4. Docs Drift

### Endpoints
- Code-only (missing from `API.md`):
  - `GET /api/ahu-heatmap`
  - `GET /api/device/{device_id}/delta-forecast`
  - `GET /api/financial-config`, `POST /api/financial-config`
  - `GET /api/on-off-periods/{ahu_id}`
  - `GET /api/ranking` (top-level; distinct from `/api/dashboard/ranking`)
  - `GET /api/safety-flags`
  - `GET /api/site/alerts`
  - `GET /api/summary`
  - `GET /api/trend`, `GET /api/trend/csv`
  - `POST /api/query`
  - Work-orders: `GET/POST/PATCH/DELETE /api/work-orders[/{wo_id}]` + actions `approve|assign|dismiss|push-to-engineers|resolve|sendback|start`
- Doc-only (no longer in code): none — every `API.md` endpoint resolves.
- Note: `API.md` is marked deprecated in favor of `docs/api-reference.md`. Confirm the new doc covers code-only list.
- Severity: `[SOON]` confirm `docs/api-reference.md` coverage; `[BACKLOG]` delete `API.md` once confirmed.

### AHU level counts
- Consistent: `AHU_LEVEL_CONFIG` in `backend/models/schemas.py` matches CLAUDE.md and `docs/CONSTITUTION.md`: L1:21 L2:15 L3:16 L4:13 L5:12 L6:11 L7:4 L8:5 L9:8 L10:8 L11:8.

### Ports
- Consistent across all docs and config: backend 8081, frontend 3000, LM Studio 1234, Influx 8086.
- Minor: `backend/main.py` docstring references `./start.sh` — verify script exists, else update to `python main.py`. `[BACKLOG]`

### Score convention
- All docs use 0–100 for user-facing health index. Internal raw signals (e.g., `bimodality_score: 0-1` in `risk_engine.py:194`, components stored 0-1 in `db_reader.py` / `score_normalize.py`) are intentional and normalized at the API boundary. No drift.

### One-liner fix candidates (in-week)
- Add code-only endpoint list to `docs/api-reference.md` (work-orders + `POST /api/query` user-impacting).
- Add removal date to `API.md` deprecation banner.
- Confirm `./start.sh` reference in `backend/main.py` docstring.

### Section 4 blockers
- None.

---

## 5. Dead Code

### Frontend unused exports (ts-prune)
- **Could not run** — ts-prune requires `frontend/tsconfig.json` (missing). Same root cause as Section 1 tsc finding. Severity: `[BLOCKER]` (blocks dead-code analysis until tsconfig restored).

### Backend unused imports/vars (F401/F841)
- `ruff check --select F401,F841 .`: **0 issues**. Severity: none.

### Stale debug scripts (>90 days)
- `scripts/debug/` contains 1 file: `test_site_summary.py`, last touched 2026-04-08 (~30 days ago). Not stale. Severity: none.

### Orphan route files
- All files in `backend/routes/*.py` referenced from `backend/main.py`. None orphaned. Severity: none.

---

## 6. Summary & Monday Backlog

### Counts by severity
- `[BLOCKER]`: 3
- `[SOON]`: 7
- `[BACKLOG]`: ~14

### In-week fixes already committed
- (none — investigation-only this sweep; blockers logged for next session)

### Top 5 backlog items for Monday triage (impact × effort)

1. **Restore `frontend/tsconfig.json`** `[BLOCKER]` — 15-min fix, unblocks tsc + ts-prune + IDE intellisense. Highest leverage. Run `npx tsc --noEmit` after to surface real type-error backlog.
2. **Fix 4 failing frontend Jest tests** `[BLOCKER]` — likely 1 regression in `utils/offPeriodAreas.tsx` (3 chart tests share assertion) + 1 in device-label format. ~1-2h. Required before any prod deploy.
3. **`ruff format .` across backend** `[SOON]` — 102 files, single command. Land as one no-op-behavior commit so future diffs stay clean.
4. **Migrate Pydantic V1 `@validator` → V2 `@field_validator` in `backend/routes/query.py`** `[SOON]` — 2 occurrences, lines 144 and 155. Pydantic V3 will remove V1 style entirely.
5. **`npm audit fix` for lodash/follow-redirects/uuid** `[SOON]` — verify in branch, run jest after. If audit-fix needs `--force`, drop to `[BACKLOG]`.

### Notable risks not in top 5
- Coverage cliffs: `api/client.ts` 23%, `store/useAppStore.ts` 0% branches. Both core surfaces.
- React 18 → 19 + Tailwind v3 → v4 + Recharts 2 → 3 are coordinated upgrades; do not piecemeal.
- `pip-audit` not installed in venv → no Python CVE visibility this sweep.
- Hardcoded InfluxDB token previously present in `scripts/fetch/wach_temp_dump.py` working tree (now stripped). Recommend rotating that token.

### Sanity check
- All findings tagged with severity. Severity tag count: see grep verification at commit time.
